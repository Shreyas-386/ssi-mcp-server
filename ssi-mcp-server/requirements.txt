# MCP protocol SDK (FastMCP).
# Pinned to 1.12.4: this is the version verified against this project's tests.
# structuredContent (used in every tool response, see the HLD's worked
# example) requires mcp>=1.10; older 1.9.x builds CallToolResult without a
# structuredContent field at all, so do not downgrade below 1.10 here.
mcp==1.12.4

# HTTP client for calling the downstream SSi REST APIs
httpx>=0.27,<1.0

# Data validation
pydantic>=2.7,<3.0
pydantic-settings>=2.2,<3.0

# Structured logging
structlog>=24.1

# .env loading (used implicitly by pydantic-settings, listed explicitly for clarity)
python-dotenv>=1.0

# HTTP transport (ASGI app + server)
starlette>=0.37
uvicorn[standard]>=0.29

# Testing
pytest>=8.0
pytest-asyncio>=0.23
respx>=0.21
